Your gateway demo is now installed!

Host: {{ .Values.ingress.host }}
Try:
  curl -i -H "Host: gateway.localhost" http://127.0.0.1/
  curl -i -H "Host: gateway.localhost" http://127.0.0.1{{ .Values.api.publicPath }}
