Your gateway demo is now installed!

Host: {{ .Values.ingress.host }}
Try:
  curl -i -H "Host: {{ .Values.ingress.host }}" http://127.0.0.1/
  curl -iL -H "Host: {{ .Values.ingress.host }}" http://127.0.0.1{{ .Values.api.publicPath }}
