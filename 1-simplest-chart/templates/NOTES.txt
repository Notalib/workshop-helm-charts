Simplest chart has been installed.

Using nginx tag {{ .Values.imageTag }}

Test Deployment:
  kubectl port-forward deploy/simplest-nginx -n {{ .Release.Namespace }} 8888:80

Exec into Deployment:
  kubectl exec -it deploy/simplest-nginx -n {{ .Release.Namespace }} -- /bin/sh

Possible values:
  `imageTag` changes the nginx tag used (default: alpine)
  `overrideHtml` flag enables overriding the default nginx HTML file (default: false)
  `myName` changes the name used in the contents of the overridden HTML.
